Rus:
Разработать класс Polinom.
Класс должен хранить полином (многочлен) от одной переменной (х).
Класс должен содержать все необходимые конструкторы, деструктор, оператор присваивания и оператор суммирования.
Класс должен предоставлять следующие операции и методы:
1) операции присваивания и суммирования
2) операцию индексации с контролем выхода индекса за границы массива.
3) метод вывода себя на консоль в текстовой форме: 1.5+4.8*x+2.4*x^2+ …
4) операции << и >> для сохранения себя в файле и чтения из файла
5) вычислить значение полинома в заданной точке х,
6) найти производную полинома.
Eng: (Google Translator)
Made the Polinom class.
A class must store a polynomial (polynomial) from a single variable (x).
The class must contain all the necessary constructors, a destructor, an assignment operator, and a sum operator.
The class must provide the following operations and methods:
1) assignment and summation operations
2) the indexing operation with the control of the index exit beyond the boundaries of the array.
3) the method of outputting itself to the console in text form: 1.5 + 4.8 * x + 2.4 * x ^ 2 + ...
4) operations << and >> to save yourself in a file and read from a file
5) calculate the value of the polynomial at a given point x,
6) find the derivative of the polynomial.